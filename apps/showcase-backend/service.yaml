apiVersion: v1
kind: Service
metadata:
  name: showcase-backend
  namespace: showcase
  labels:
    app: showcase-backend
spec:
  selector:
    app: showcase-backend
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  type: ClusterIP
